# M16.T1: .mcp.json Generator

## Refs
- Architecture: plan/architecture/bridge-mcp.md, .mcp.json Configuration
- Task def: plan/implementation/phase-4-tasks.md, Task 4.10
- Modules: src/claude_bridge/cli.py, src/claude_bridge/bridge_bot_claude_md.py
- Dependencies: M15

## What This Task Does
Update `setup` command to generate both CLAUDE.md and .mcp.json for the bridge-bot project. The .mcp.json configures Bridge MCP server as a stdio MCP server.

## Test Plan
- setup generates valid .mcp.json with bridge server config
- .mcp.json includes correct python path and PYTHONPATH
- .mcp.json includes TELEGRAM_BOT_TOKEN from config
- setup-bot command creates both files in target directory
