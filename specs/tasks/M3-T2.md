# M3.T2: Implement status Command

## Refs
- Architecture: plan/architecture/bridge-cli.md, section 2
- Task def: plan/implementation/phase-1-tasks.md, Task 3.2
- Modules: src/claude_bridge/cli.py (cmd_status)
- Dependencies: M3.T1

## What This Task Does
Test status command: shows agent state, running task details, PID liveness check.

## Key Architecture Constraints
- Should verify PID is still alive via os.kill(pid, 0)
- If PID is dead but task still marked running, update status

## Test Plan
Happy path:
- Status with no running tasks
- Status with name shows agent details

Edge cases:
- Status for non-existent agent returns error

## Gaps in Existing Code
- cmd_status does not verify PID is alive — needs fix
